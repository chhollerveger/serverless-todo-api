export enum StatusCode {
  Success = 200,
  BadRequest = 400,
  NotFound = 404,
  ServerError = 500,
}

export enum StatusName {
  Success = 'Success',
  BadRequest = 'Bad Request',
  NotFound = 'Not Found',
  ServerError = 'Server Error',
}